
class Renderer {
/*
 *    Window window;
 *    Camera camera;
 *    Array!(GameObject) objects;
 *
 *    this(Window window, Camera camera) {
 *        this.window = window;
 *        this.camera = camera;
 *    }
 *
 *    void draw() {
 *        for (obj : objects)
 *            obj.draw();
 *        window.flip();
 *    }
 */
}
